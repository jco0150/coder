import React from 'react'

const Layout = ( {children} ) => {

    
  return (
    <div>
        {children}
        <h2 style={{backgroundColor: "#FFCEFE"}}>Este Seria EL footer</h2>
    </div>
  )
}

export default Layout